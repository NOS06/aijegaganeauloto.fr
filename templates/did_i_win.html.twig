{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block main %}
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form) }}
    <button type="submit">
        Launch
    </button>
    {{ form_end(form) }}
{% endblock %}
